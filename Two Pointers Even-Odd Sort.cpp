#include <iostream>
using namespace std;

void merge(int* p, int* q) {}

int main() {      //p             q
    int arr[10] = { 2, 4, 8, 10, 12, 1, 3, 5, 7, 9 };
    int* p;
    int* q;
    merge(p, q);
}